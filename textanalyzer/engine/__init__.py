"""Engine package — clustering and word-cloud machinery.

Modules in this package replace the original root-level ``cluster_tool.py``
and ``wordcloud_tool.py``. The root files now exist as backwards-compat
shims that re-export everything here.
"""
